# ─── DATABASE (Railway auto-sets this when you add PostgreSQL) ─────────────────
DATABASE_URL=postgresql://postgres:password@localhost:5432/ai_assistant

# ─── SECURITY ─────────────────────────────────────────────────────────────────
JWT_SECRET=replace-with-64-random-characters-minimum-do-not-share
SETUP_KEY=replace-with-secret-to-create-your-admin-account
ENCRYPTION_KEY=replace-with-32-random-characters-for-encrypting-customer-data

# ─── STRIPE ───────────────────────────────────────────────────────────────────
STRIPE_SECRET_KEY=sk_live_xxxxxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxx
STRIPE_PRICE_ID=price_xxxxxxxxxxxx

# ─── RAILWAY API (to provision OpenClaw instances per customer) ────────────────
RAILWAY_API_TOKEN=xxxxxxxxxxxx
RAILWAY_PROJECT_ID=xxxxxxxxxxxx
MASTER_API_URL=https://your-api.up.railway.app

# ─── TWILIO (WhatsApp + Voice calls) ────────────────────────────────────────
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxx
TWILIO_PHONE_NUMBER=+1xxxxxxxxxx
# WhatsApp webhook: configure each Twilio WhatsApp number's inbound webhook to:
#   POST https://{MASTER_API_URL}/webhook/twilio

# ─── ANTHROPIC (Claude AI — used by each OpenClaw instance) ───────────────────
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxx

# ─── EMAIL (SMTP — platform-level email sending) ─────────────────────────────
# Used when customer hasn't configured their own Gmail. Can be Gmail with app password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# ─── WEB SEARCH ──────────────────────────────────────────────────────────────
# At least one is required for the search tool to work.
BRAVE_SEARCH_API_KEY=BSAxxxxxxxxxxxx
# SERP_API_KEY=xxxxxxxxxxxx

# ─── GOOGLE CALENDAR (OAuth2 for per-customer calendar access) ───────────────
GOOGLE_CLIENT_ID=xxxxxxxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=xxxxxxxxxxxx
GOOGLE_REDIRECT_URI=https://your-api.up.railway.app/api/tools/calendar/callback

# ─── APP ──────────────────────────────────────────────────────────────────────
FRONTEND_URL=https://your-dashboard.up.railway.app
PORT=8080
NODE_ENV=production
